#include <stdio.h>
#include <math.h>
int main()
{
    int a = 4 //; float b=8.9;
    // printf("the value of a+b is = %d \n", a + b);
    // printf("the value of a-b is = %d \n", a - b);
    // printf("the value of a*b is = %d \n", a * b);
    // printf("the value of a/b is = %d \n", a / b);
    // printf("the value of a/b is = %f \n", pow(2, 3));
    // printf("the value of a+b is = %d \n", a / b);
    return 0;
}